<h1>All Post</h1>

<a href = "<%=new_post_url%>">New Post</a>

<%@posts.each_with_index do |post, i|%>
	<p><%=i+1%>. <a href = "<%=post_url(post)%>"><%=post.body%></a>
	</p>
	<%post.links.each_with_index do |link, j|%>
		<p>&nbsp&nbsp<%=j+1%>. <%=link.url%>
		</p>
	<%end%>
<%end%>

